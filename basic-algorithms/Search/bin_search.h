''' binary search '''

#ifndef __BIN_SEARCH_H
#define __BIN_SEARCH_H

#include <vector>

int find(std::vector<int> &arr, int x);
std::vector<int> find_k_sum(std::vector<int> &arr, int sum);

#endif
