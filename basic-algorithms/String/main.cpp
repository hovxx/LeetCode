/* main process */

#include "string.h"

#include <iostream>


int
main(int argc, char **argv)
{
  string str1 = "Kobe Bryant is the best NBA player in the history!";
  string str2 = "Kobe";
  string str3 = "KobeeboK";
  string str4 = "Kobe and Lonzo, who is better? I think I will choose Kobe.";
  string a = "100010";
  string b = "100";

  // reverse_string(str1);
  // reverse_words_in_string(str1);

  // move_blank(str1);
  // vector<string> ans = split(str1);
  // split_c((char *)str1.c_str());

  // vector<string> ans = permute(str2);

  // printf("%d %d\n", is_palindrome(str2), is_palindrome(str3));

  // vector<int> ans = match(str4, str2);

  // string ans = add_binary(a, b);

  return 0;
}
